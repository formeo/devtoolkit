export const metadata = { title: 'URL Encoder / Decoder Online', description: 'Encode or decode URL components with full Unicode support.', alternates: { canonical: '/url-encoder/' } };
import Client from './client';
export default function Page() { return <Client />; }
