export const metadata = { title: 'URL Encoder / Decoder Online', description: 'Free online URL encoder and decoder. Encode special characters and Unicode in URLs, decode percent-encoded strings. Essential for API and web development.', alternates: { canonical: '/url-encoder/' } };
import Client from './client';
export default function Page() { return <Client />; }
