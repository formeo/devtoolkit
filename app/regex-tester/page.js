export const metadata = { title: 'Regex Tester — Test Regular Expressions Online', description: 'Free regex tester. Test regular expressions in real-time with match highlighting, groups, and flags. JavaScript regex.', keywords: ['regex tester', 'regex online', 'regular expression tester', 'regex debugger', 'javascript regex'],
  alternates: { canonical: '/regex-tester/' } };
import Client from './client';
export default function Page() { return <Client />; }
