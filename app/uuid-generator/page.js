export const metadata = { title: 'UUID Generator — Random UUID v4 Online', description: 'Free UUID v4 generator. Cryptographically secure random UUIDs in multiple formats. Bulk generation, one-click copy. Uses crypto.getRandomValues().', alternates: { canonical: '/uuid-generator/' }, keywords: ['uuid generator', 'uuid v4', 'random uuid online', 'guid generator', 'unique id generator'] };
import Client from './client';
export default function Page() { return <Client />; }
