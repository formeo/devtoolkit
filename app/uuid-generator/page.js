export const metadata = { title: 'UUID Generator — Random UUID v4 Online', description: 'Generate random UUID v4 identifiers in multiple formats.', alternates: { canonical: '/uuid-generator/' } };
import Client from './client';
export default function Page() { return <Client />; }
