export const metadata = { title: 'JWT Decoder — Decode JWT Tokens Online', description: 'Free online JWT decoder. Decode and inspect JSON Web Tokens instantly. View header, payload, claims, and expiration. No data sent to server.', alternates: { canonical: '/jwt-decoder/' }, keywords: ['jwt decoder', 'decode jwt token', 'jwt parser online', 'json web token decoder', 'jwt debugger'] };
import Client from './client';
export default function Page() { return <Client />; }
