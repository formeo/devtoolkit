export const metadata = { title: 'JWT Decoder — Decode JWT Tokens Online', description: 'Free JWT decoder. Inspect JSON Web Tokens instantly — header, payload, claims, expiration. No data sent to server.', keywords: ['jwt decoder', 'decode jwt token', 'jwt parser online', 'json web token decoder', 'jwt debugger'],
  alternates: { canonical: '/jwt-decoder/' } };
import Client from './client';
export default function Page() { return <Client />; }
