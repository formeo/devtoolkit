export const metadata = { title: 'Base64 Encoder / Decoder — Text & Files', description: 'Free online Base64 encoder and decoder. Encode text or files (images, PDFs, any binary) to Base64 and back. Drag & drop support, UTF-8, data URI output. No upload — runs in browser.', alternates: { canonical: '/base64-encoder/' }, keywords: ['base64 encoder', 'base64 decoder', 'file to base64', 'image to base64', 'base64 online', 'data uri generator'] };
import Client from './client';
export default function Page() { return <Client />; }
