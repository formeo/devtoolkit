export const metadata = { title: 'Base64 Encoder / Decoder Online', description: 'Free online Base64 encoder and decoder with full UTF-8 support.', alternates: { canonical: '/base64-encoder/' } };
import Client from './client';
export default function Page() { return <Client />; }
